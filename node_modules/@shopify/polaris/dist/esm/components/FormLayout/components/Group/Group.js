import React$1, { Children } from 'react';
import { useUniqueId } from '../../../../utilities/unique-id/hooks.js';
import { classNames } from '../../../../utilities/css.js';
import { wrapWithComponent } from '../../../../utilities/components.js';
import styles from '../../FormLayout.scss.js';
import { Item as Item$1 } from '../Item/Item.js';

function Group({
  children,
  condensed,
  title,
  helpText
}) {
  var className = classNames(condensed ? styles.condensed : styles.grouped);
  var id = useUniqueId('FormLayoutGroup');
  var helpTextElement = null;
  var helpTextID;
  var titleElement = null;
  var titleID;

  if (helpText) {
    helpTextID = "".concat(id, "HelpText");
    helpTextElement = /*#__PURE__*/React$1.createElement("div", {
      id: helpTextID,
      className: styles.HelpText
    }, helpText);
  }

  if (title) {
    titleID = "".concat(id, "Title");
    titleElement = /*#__PURE__*/React$1.createElement("div", {
      id: titleID,
      className: styles.Title
    }, title);
  }

  var itemsMarkup = Children.map(children, child => wrapWithComponent(child, Item$1, {}));
  return /*#__PURE__*/React$1.createElement("div", {
    role: "group",
    className: className,
    "aria-labelledby": titleID,
    "aria-describedby": helpTextID
  }, titleElement, /*#__PURE__*/React$1.createElement("div", {
    className: styles.Items
  }, itemsMarkup), helpTextElement);
}

export { Group };
