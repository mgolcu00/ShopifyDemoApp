import React$1, { memo } from 'react';
import { classNames } from '../../../../utilities/css.js';
import styles from '../../DatePicker.scss.js';

var Weekday = /*#__PURE__*/memo(function Weekday({
  label,
  title,
  current
}) {
  var className = classNames(styles.Weekday, current && styles['Weekday-current']);
  return /*#__PURE__*/React$1.createElement("div", {
    "aria-label": label,
    className: className
  }, title);
});

export { Weekday };
